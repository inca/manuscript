export interface RenderOptions {
    title: string;
    themeColor: string;
    lang: string;
    description: string;
    charset: string;
    favicon: string;
    isProduction: boolean;
    [key: string]: any;
}
